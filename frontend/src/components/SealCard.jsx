import React from 'react'

/**
 * SealCard — Compact card: persona, tool, 4 gate dots, status badge, CoT toggle
 *
 * TODO: Implement per NEXUS_BUILD_SPEC.md Phase 11
 */
export default function SealCard({ data }) {
  return (
    <div className="border border-gray-700 rounded-lg p-4 bg-gray-900">
      <p className="text-gray-400">SealCard: Compact card: persona, tool, 4 gate dots, status badge, CoT toggle</p>
    </div>
  )
}
